# Use this to set up a virtual environment with all dependencies, except for the python dependencies.
name: ibots-site
channels:
  - defaults
dependencies: 
  - pip
  - python=3.12
  - nodejs
  - pip:
    - -e ../static_site_generator
    - dvc
    - dvc_webdav
    - dvc-gdrive

  





