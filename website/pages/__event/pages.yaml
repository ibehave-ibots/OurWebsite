
{% for event_type, series in data.events.items() %}
{% for event_id, event in series.items() %}
- url: /events/{{ event_type | replace('_', '-') }}/{{ event_id }}.html
  data:
    id: {{ event_id }}
    type: {{ event_type }}
{% endfor %}
{% endfor %}